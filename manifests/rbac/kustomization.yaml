apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Spans argo / argo-l3 / runbook-demo-dev -- resources carry their own namespaces
# (runners.yaml creates argo-l3 and runbook-demo-dev), so no namespace transform here.
resources:
  - runners.yaml
  - humans.yaml
